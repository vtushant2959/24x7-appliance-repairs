import type { GalleryImage } from "./types";

/**
 * Intentionally empty until real work photos are supplied.
 * Gallery reads this array and renders nothing until it has real entries —
 * never seed this with generic stock photography.
 */
export const gallery: GalleryImage[] = [];
