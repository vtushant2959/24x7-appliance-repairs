import type { TeamMember } from "./types";

/**
 * Intentionally empty until real technician photos/profiles are supplied.
 * TeamSection reads this array and renders nothing until it has real entries -
 * never seed this with stock photos or fabricated staff bios.
 */
export const team: TeamMember[] = [];
