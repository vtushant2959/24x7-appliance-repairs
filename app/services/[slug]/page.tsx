import { notFound } from "next/navigation";
import { ServiceContent } from "@/components/ServiceContent";
import { JsonLd } from "@/components/ui";
import { getService, services } from "@/lib/data";
import { breadcrumbSchema, faqSchema, pageMetadata } from "@/lib/seo";
import { site } from "@/lib/site";
export function generateStaticParams() { return services.map((service) => ({ slug: service.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const service = getService(slug); if (!service) return {}; return pageMetadata({ title: `${service.name} Near You`, description: `Book same day doorstep ${service.name.toLowerCase()} across Delhi NCR with trained technicians, transparent estimates and major brand coverage.`, path: `/services/${service.slug}`, image: service.image }); }
export default async function ServicePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const service = getService(slug); if (!service) notFound(); const f = [{ question: `Do you offer ${service.name.toLowerCase()} near me?`, answer: `Yes, The Home Appliance Services offers doorstep ${service.name.toLowerCase()} across Faridabad, South Delhi, Noida, Greater Noida, Gurugram, Ghaziabad, Ballabhgarh and Palwal.` }]; return <main><JsonLd data={breadcrumbSchema([{ name: "Home", url: site.url }, { name: "Services", url: `${site.url}/services` }, { name: service.name, url: `${site.url}/services/${service.slug}` }])} /><JsonLd data={{ "@context": "https://schema.org", "@type": "Service", name: service.name, serviceType: service.name, provider: { "@type": "LocalBusiness", name: site.name, telephone: site.phonePrimary }, areaServed: ["Faridabad", "South Delhi", "Noida", "Greater Noida", "Gurugram", "Ghaziabad", "Ballabhgarh", "Palwal"] }} /><JsonLd data={faqSchema(f)} /><section className="bg-brand-mist py-14 dark:bg-slate-900"><div className="container-page"><p className="font-bold text-brand-blue">Doorstep service</p><h1 className="mt-3 max-w-4xl text-4xl font-black text-brand-navy dark:text-white">{service.name}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-700 dark:text-slate-300">Fast, professional and transparent {service.name.toLowerCase()} for Delhi NCR homes.</p></div></section><ServiceContent service={service} /></main>; }