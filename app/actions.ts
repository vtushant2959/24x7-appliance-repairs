"use server";
import nodemailer from "nodemailer";
import { z } from "zod";
const leadSchema = z.object({ name: z.string().min(2), phone: z.string().min(8), service: z.string().min(2), area: z.string().min(2), message: z.string().optional() });
export async function submitLead(input: unknown) { const parsed = leadSchema.safeParse(input); if (!parsed.success) return { ok: false, message: "Please enter your name, phone, service and area." }; const lead = parsed.data; if (!process.env.SMTP_HOST || !process.env.LEAD_TO_EMAIL) { console.info("Lead received", lead); return { ok: true, message: "Thanks. Your request is saved. Our team will call you shortly." }; } const transporter = nodemailer.createTransport({ host: process.env.SMTP_HOST, port: Number(process.env.SMTP_PORT || 587), secure: process.env.SMTP_SECURE === "true", auth: process.env.SMTP_USER ? { user: process.env.SMTP_USER, pass: process.env.SMTP_PASS } : undefined }); await transporter.sendMail({ from: process.env.LEAD_FROM_EMAIL || process.env.LEAD_TO_EMAIL, to: process.env.LEAD_TO_EMAIL, subject: `New appliance repair lead: ${lead.service}`, text: `Name: ${lead.name}\nPhone: ${lead.phone}\nService: ${lead.service}\nArea: ${lead.area}\nMessage: ${lead.message || ""}` }); return { ok: true, message: "Thanks. Your booking request has been sent." }; }